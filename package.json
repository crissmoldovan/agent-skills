{
  "name": "@crissmoldovan/agent-skills",
  "version": "0.13.1",
  "description": "Verified Agent Skill publication, routing, lifecycles, Blocks reviews, and secure credentials.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --test test/*.mjs",
    "verify:skills": "node scripts/verify-skills.mjs",
    "prepare:lifecycle": "npm ci --prefix packages/agent-lifecycle --ignore-scripts --no-audit --no-fund",
    "verify:lifecycle": "npm run prepare:lifecycle && npm --prefix packages/agent-lifecycle run verify",
    "prepare:journal": "npm ci --prefix packages/agent-journal --ignore-scripts --no-audit --no-fund",
    "verify:journal": "npm run prepare:journal && npm --prefix packages/agent-journal run verify",
    "prepare:governance": "npm ci --prefix packages/workspace-governance --ignore-scripts --no-audit --no-fund",
    "verify:governance": "npm run prepare:governance && npm --prefix packages/workspace-governance run verify",
    "verify": "npm test && node scripts/verify-skills.mjs && node scripts/verify-digests.mjs && node scripts/verify-journal-bundle.mjs && npm run verify:lifecycle && npm run verify:journal && npm run verify:governance"
  },
  "license": "MIT"
}
