{
  "name": "@crissmoldovan/agent-skills",
  "version": "0.3.0",
  "description": "Token-efficient model routing and observable agent lifecycles.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --test test/*.mjs",
    "verify:skills": "node scripts/verify-skills.mjs",
    "verify:lifecycle": "npm --prefix packages/agent-lifecycle run verify",
    "verify": "npm test && node scripts/verify-skills.mjs && npm run verify:lifecycle"
  },
  "license": "MIT"
}
