{
  "name": "@crissmoldovan/agent-skills",
  "version": "0.4.0",
  "description": "Model routing, agent lifecycles, and Blocks reviews.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --test test/*.mjs",
    "verify:skills": "node scripts/verify-skills.mjs",
    "prepare:lifecycle": "npm ci --prefix packages/agent-lifecycle --ignore-scripts --no-audit --no-fund",
    "verify:lifecycle": "npm run prepare:lifecycle && npm --prefix packages/agent-lifecycle run verify",
    "verify": "npm test && node scripts/verify-skills.mjs && npm run verify:lifecycle"
  },
  "license": "MIT"
}
