# Runbook

**Status: configured, not deployed.** Nothing runs in production yet.

## Deploying

1. `npm run build`
2. `npm run deploy -- --env staging --dry-run`
3. Read the plan, then run it again without `--dry-run`.
