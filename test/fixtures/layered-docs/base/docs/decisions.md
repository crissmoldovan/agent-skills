# Decisions

Superseded, never rewritten. Each entry keeps its date.

## 2026-04-02 — one registry file, generated

The registry is generated into `generated/registry.json` rather than maintained
by hand, because two people editing it by hand produced three merge conflicts in
one week.
