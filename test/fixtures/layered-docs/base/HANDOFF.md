# Start here

This is the file to read first when picking up widget-service.

The service is **not deployed yet** — staging is configured and production is
waiting on the account. The next person should finish the deploy.

Credentials for the staging run are in the shared vault. The value currently in
use is `wgt_live_EXAMPLEEXAMPLEEXAMPLEEXAMPLE00`.
