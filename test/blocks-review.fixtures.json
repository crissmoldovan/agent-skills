[
  {
    "id": "real-A",
    "expected": "findings",
    "body": "Reviewed PR #29 and left one inline comment on the tracing guard.\n\nThe new guard can approve a shared helper under `app/` even when it is imported by a dynamic route, leaving the untraced `public/skills/` read unsafe.\n\nI couldn't run the Vitest command locally because `pnpm` is unavailable in this environment.\n\n**[View on dashboard](https://blocks.team/app/dd73b09d/sessions/b549a6cc)**",
    "source": "observed on cueplusplus/skills#29"
  },
  {
    "id": "real-B",
    "expected": "clean",
    "body": "Reviewed PR #29 at `4248476`.\n\nThe route-entry filename guard closes the previously reported shared-helper bypass. I found no new severity \u22657 issues, so I left no additional PR comments.\n\nFocused local test execution wasn't possible because this checkout has neither `pnpm` nor dependencies installed; the branch is clean and `git diff --check` passes.\n\n**[View on dashboard](https://blocks.team/app/dd73b09d/sessions/bc5ccafb)**",
    "source": "observed on cueplusplus/skills#29"
  },
  {
    "id": "real-C",
    "expected": "clean",
    "body": "Reviewed `67b6d36` and its documentation-only diff.\n\nNo new severity \u22657 findings. The current CI run is green for this exact head SHA, and `git diff --check` passes. I left no PR comments.\n\n**[View on dashboard](https://blocks.team/app/dd73b09d/sessions/5b459a6a)**",
    "source": "observed on cueplusplus/skills#29"
  },
  {
    "id": "real-D",
    "expected": "clean",
    "body": "Reviewed PR #29 at `7d8bb2c`.\n\nNo new severity \u22657 findings; no PR comments posted. The documentation amendment matches the recorded PR discussion, and `git diff --check` passes. Current head CI is successful.\n\n**[View on dashboard](https://blocks.team/app/dd73b09d/sessions/8c43ac8a)**",
    "source": "observed on cueplusplus/skills#29"
  },
  {
    "id": "real-E",
    "expected": "nonterminal",
    "body": "Mention Blocks like a regular teammate with your question or request:\n\n@blocks review this pull request\n@blocks make the following changes ...\n\nRun `@blocks /help` for more information.",
    "source": "observed on cueplusplus/skills#29"
  },
  {
    "id": "real-F",
    "expected": "nonterminal",
    "body": "I'm working on this request and will respond here shortly.",
    "source": "observed on cueplusplus/skills#29"
  },
  {
    "id": "real-G",
    "expected": "nonterminal",
    "body": "The latest commit is documentation-only. I'm comparing it directly with the already-reviewed round-three head and checking whether the recorded claims match the discussion.",
    "source": "observed on cueplusplus/skills#29"
  },
  {
    "id": "gen-0",
    "expected": "nonterminal",
    "body": "I've picked up this request and will report back on this comment when the pass finishes.\n\n**[View on dashboard](https://blocks.team/runs/8f41c0)**",
    "source": "generated",
    "why": "A bare courtesy acknowledgement with none of the hardcoded courtesy phrases (no \"taking a look\", no \"queued\"), so it stresses whether a repair still refuses to read an unfinished pass as a verdict when it falls through to the default branch."
  },
  {
    "id": "gen-1",
    "expected": "nonterminal",
    "body": "Queued behind two other runs in this repository. I'll start on `b7d3f19` as soon as a worker frees up; it usually takes a few minutes.\n\n**[View on dashboard](https://blocks.team/runs/8f41c1)**",
    "source": "generated",
    "why": "A plain queued notice that names a sha and a timing estimate; it is the baseline courtesy case that must keep landing in 'reviewing' even after verdict detection is loosened to catch bodies like [C]."
  },
  {
    "id": "gen-2",
    "expected": "nonterminal",
    "body": "Working through `29a4f0c`:\n\n- [x] Fetched the diff and the merge base\n- [x] Read `app/api/tracing/route.ts`\n- [ ] Read `packages/guards/src/entry.ts`\n- [ ] Review complete\n\nI edit this comment as each step lands.",
    "source": "generated",
    "why": "A streamed progress checklist whose last, explicitly unchecked item is the literal string \"Review complete\", which today trips isVerdict and yields a false 'clean' that would accept an unreviewed head."
  },
  {
    "id": "gen-3",
    "expected": "nonterminal",
    "body": "Reviewed PR #29 up to `packages/guards/`, which is about half of the diff. The remaining files touch the queue worker and I need another pass over them before I can say anything.",
    "source": "generated",
    "why": "It opens with the literal verdict trigger \"Reviewed PR\" while saying the pass is half done, so today isVerdict fires, no findings word survives, and it becomes a false 'clean' on a partially reviewed diff."
  },
  {
    "id": "gen-4",
    "expected": "nonterminal",
    "body": "So far the migration in `supabase/migrations/20260901_add_route_entry.sql` looks good; I still have the worker changes and the Vitest suite to get through before I can post a verdict.",
    "source": "generated",
    "why": "\"looks good\" satisfies both halves of isClean even though the sentence explicitly defers the verdict, so a mid-flight impression is currently promoted to a terminal 'clean'."
  },
  {
    "id": "gen-5",
    "expected": "nonterminal",
    "body": "I have no findings to report yet - I have read two of the nine changed files. This is not a verdict; I will post one when the pass on `4d90ab2` finishes.",
    "source": "generated",
    "why": "\"no findings\" plus \"yet\" is the highest-value trap in the set: both isClean clauses match and the comment classifies 'clean' today, despite stating outright that seven files are unread."
  },
  {
    "id": "gen-6",
    "expected": "nonterminal",
    "body": "Mention me in a comment to get started:\n\n`@blocks review this pull request`\n`@blocks fix the findings you left`\n\nWhen a pass finishes I reply with **Reviewed PR #<number>** and either the findings I left inline or `No actionable findings.` Run `@blocks /help` for the full command list.",
    "source": "generated",
    "why": "Onboarding text that documents the bot's own terminal vocabulary \u2014 it quotes \"Reviewed PR\", \"findings\" and \"No actionable findings\" \u2014 and currently classifies as 'findings', sending an agent hunting for a finding that was never made."
  },
  {
    "id": "gen-7",
    "expected": "nonterminal",
    "body": "Round three ended with *No new severity >=7 findings*, so I am starting from that head and only looking at what `c0ffee1` changed on top of it. Still reading.",
    "source": "generated",
    "why": "A progress note that quotes the previous round's verdict verbatim, so the [C]/[D] phrase survives negation-stripping and any repair that keys terminality off quoted verdict language would misread it."
  },
  {
    "id": "gen-8",
    "expected": "nonterminal",
    "body": "Next I will check whether there are findings in the tracing guard, once `pnpm install` finishes; the sandbox is still restoring its dependency cache.\n\n**[View on dashboard](https://blocks.team/runs/8f41c9)**",
    "source": "generated",
    "why": "Future-tense intent to look for findings; reportsFindings already returns true on the bare mention, so it stresses that a findings mention alone must never be sufficient evidence that a review finished."
  },
  {
    "id": "gen-9",
    "expected": "nonterminal",
    "body": "My previous run on this pull request was interrupted while fetching the diff (GitHub returned `403 rate limit exceeded`). I am retrying against `9f2c1aa` now - nothing I posted earlier should be read as a verdict.",
    "source": "generated",
    "why": "A retry-after-failure notice containing \"pull request\" and \"verdict\"; it must stay nonterminal because the earlier comment it disavows may itself have looked terminal to the classifier."
  },
  {
    "id": "gen-10",
    "expected": "nonterminal",
    "body": "Before I finish I need to know whether `scripts/legacy/` is in scope for this review: the repository config excludes it, but the PR description asks for it explicitly. I will hold here until someone replies.",
    "source": "generated",
    "why": "The review is blocked on a human answer rather than finished, and it carries no verdict vocabulary at all, so it is the baseline that confirms 'requested' still absorbs genuinely stalled states."
  },
  {
    "id": "gen-11",
    "expected": "nonterminal",
    "body": "I am about to complete the review of `7d8bb2c`. The last step is re-running `git diff --check` and confirming the CI conclusion for this exact head; a second comment will follow.",
    "source": "generated",
    "why": "\"complete the review\" is one word order away from the \"review complete\" trigger, so it stresses any repair that loosens the phrase into a proximity or `review.*complete` match."
  },
  {
    "id": "gen-12",
    "expected": "nonterminal",
    "body": "Running analysis on `a1c9e88` (7 of 19 files scanned). I have left no inline comments so far and I have not concluded anything about the queue worker changes.\n\n**[View on dashboard](https://blocks.team/runs/8f41d3)**",
    "source": "generated",
    "why": "It contains isClean's second-clause phrase \"left no inline comments\" mid-scan, so a repair that promotes that phrase to standalone clean evidence would accept a review that is 7 of 19 files in."
  },
  {
    "id": "gen-13",
    "expected": "nonterminal",
    "body": "Thanks - I can see the new commit `56ee31b`. I am re-reviewing only the files that changed since my last pass; the earlier findings stay open until I have confirmed they are gone.",
    "source": "generated",
    "why": "A re-review kickoff that references still-open findings from a prior round: issues exist but this pass has not finished, so it must not be read as the re-review's own 'findings' verdict."
  },
  {
    "id": "gen-14",
    "expected": "nonterminal",
    "body": "Reviewed `67b6d36` down to the worker package so far. I am now pulling the CI logs for that head and will read `packages/queue/` after that, so please treat this as progress rather than a result.",
    "source": "generated",
    "why": "It opens exactly like the misclassified true-clean body [C] \u2014 \"Reviewed `sha`\" \u2014 so it is the direct counterweight to the obvious fix of treating \"Reviewed <sha>\" as a verdict opener."
  },
  {
    "id": "gen-15",
    "expected": "nonterminal",
    "body": "I have two candidate findings written up locally - both in `packages/guards/src/entry.ts` - but I have not posted them yet, and four files are still unread. Nothing is final until I say so.\n\n**[View on dashboard](https://blocks.team/runs/8f41e2)**",
    "source": "generated",
    "why": "A counted findings phrase (\"two candidate findings\") in an unfinished pass with nothing posted, which would trip any counting heuristic into a premature 'findings' and send an agent to fix half a review."
  },
  {
    "id": "gen-16",
    "expected": "nonterminal",
    "body": "The first commit `3b1f0aa` is LGTM on its own, but this branch has three more commits on top of it that I have not opened yet.",
    "source": "generated",
    "why": "A scoped \"LGTM\" about one commit satisfies both isClean clauses and classifies 'clean' today, even though the sentence says most of the branch is unread."
  },
  {
    "id": "gen-17",
    "expected": "clean",
    "body": "Reviewed `c0d41ab` on `feat/rate-limit-window`.\n\nThe diff is confined to `src/lib/rate-limit.ts` and its unit test. The sliding-window reset now uses a monotonic clock, and the two callers in `app/api/ingest/route.ts` pass the same bucket key they did before. Nothing at or above severity 7 turned up, so I posted nothing inline.\n\nI could not execute the suite here because dependencies are not installed in this checkout, but CI for this exact head SHA is green and `git diff --check` passes.\n\n**[View on dashboard](https://blocks.team/beamtech/pr/41/runs/8812)**",
    "source": "generated",
    "why": "A finished review reporting nothing outstanding, but it opens with a bare SHA and never says \"findings\", \"LGTM\" or \"pull request\" \u2014 the exact [C] seam, and today it falls all the way through to nonterminal 'requested'."
  },
  {
    "id": "gen-18",
    "expected": "clean",
    "body": "Review complete for PR #41 at `c0d41ab`.\n\n## Findings\n\nNone.\n\n## Notes\n\nThe retry wrapper now forwards the caller's `AbortSignal`, which closes the leak reported two rounds ago. Error mapping in `src/lib/http.ts` is unchanged. I left no inline comments on this head.\n\n**[View on dashboard](https://blocks.team/beamtech/pr/41/runs/8830)**",
    "source": "generated",
    "why": "The zero is expressed as a section heading plus \"None.\", so the negation-stripper never fires and the bare-mention fallback turns a structured all-clear report into a false 'findings'."
  },
  {
    "id": "gen-19",
    "expected": "clean",
    "body": "Finished reviewing `7fe0aa1`.\n\nThe two findings I raised on the previous head were fixed: the migration is now wrapped in a transaction, and the seed script no longer swallows the rejection. Zero blocking issues remain on this head, so there is nothing for you to act on.\n\nI verified the diff by reading it rather than running it; `pnpm` is unavailable in this environment.",
    "source": "generated",
    "why": "\"Finished reviewing\" is a natural terminal opener that no verdict pattern recognises, and \"zero blocking issues\" avoids every clean phrase, so a completed review reads as not-yet-started."
  },
  {
    "id": "gen-20",
    "expected": "clean",
    "body": "Reviewed PR #58 at `2b19d0e`.\n\nI found nothing actionable in the diff. The new `useDebouncedQuery` hook cancels its in-flight request on unmount, and the memo key includes every input it reads. The rename of `lib/cache.ts` to `lib/cache/index.ts` keeps all four import sites correct.\n\n**[View on dashboard](https://blocks.team/beamtech/pull/58/findings)**",
    "source": "generated",
    "why": "The only occurrence of \"findings\" in the whole body is a path segment inside the dashboard URL, which the bare-mention fallback happily counts as an outstanding finding."
  },
  {
    "id": "gen-21",
    "expected": "clean",
    "body": "I have reviewed this pull request end to end at `4c7db90`.\n\nNo new severity >=7 findings. The auth middleware short-circuits before the session lookup for public routes, which is the behaviour the issue asked for, and the added test covers both branches. I posted no inline comments.\n\nLocal test execution was not possible in this environment, but the head CI run is successful.",
    "source": "generated",
    "why": "It hits the first half of the clean test but says \"posted no inline comments\" instead of the hardcoded \"left no inline comments\", and \"No new severity >=7 findings\" is three tokens wide \u2014 so a plainly finished, plainly empty review is nonterminal."
  },
  {
    "id": "gen-22",
    "expected": "clean",
    "body": "Review complete for `9ac1f02`.\n\nThe preview deployment that was queued at push time finished successfully, and I read its build log alongside the diff. Zero findings at severity 7 or above; I posted nothing inline and requested no changes.\n\nOne observation for later, not blocking: `docs/deploy.md` still references the old preview URL scheme.\n\n**[View on dashboard](https://blocks.team/beamtech/pr/61/runs/9101)**",
    "source": "generated",
    "why": "An explicit \"Review complete\" is cancelled because the word \"queued\" appears in an incidental sentence about CI, demoting a terminal verdict to nonterminal 'reviewing' \u2014 the failure mode that burns the whole timeout."
  },
  {
    "id": "gen-23",
    "expected": "clean",
    "body": "Reviewed PR #61 at `ff30b18`.\n\nNothing at or above severity 7 in this diff. The Playwright specs were split into three shards, which is a config-only change, and the fixture paths all resolve.\n\nThe full end-to-end suite may take a few minutes to finish in CI, but the unit and lint jobs are already green for this head.",
    "source": "generated",
    "why": "\"may take a few minutes\" describes someone else's CI job, not the review, yet it vetoes the \"reviewed PR\" verdict and drops a finished all-clear into nonterminal 'reviewing'."
  },
  {
    "id": "gen-24",
    "expected": "clean",
    "body": "Re-review complete for `d55c1e0`.\n\nAll three findings from the first round are resolved: the unbounded `Promise.all` is now chunked, the error is rethrown instead of logged, and the fixture cleanup runs in `afterEach`. My earlier inline comments have been marked outdated by GitHub because the lines they anchored to were rewritten.\n\nThis round produced no findings of its own, and I left no new inline comments.\n\n**[View on dashboard](https://blocks.team/beamtech/pr/44/runs/9204)**",
    "source": "generated",
    "why": "Mentions findings and inline comments four times while reporting none outstanding; the resolved-findings clause strips cleanly but \"inline comments have been marked outdated\" uses a verb outside the fixed/resolved/addressed/cleared list and survives into a false 'findings'."
  },
  {
    "id": "gen-25",
    "expected": "clean",
    "body": "Reviewed `81ae44c`.\n\n0 findings at severity >= 7. The change deletes a dead feature flag and its two branches, and nothing else reads `ENABLE_LEGACY_UPLOAD`. LGTM.\n\n**[View on dashboard](https://blocks.team/beamtech/pr/70/runs/9310)**",
    "source": "generated",
    "why": "Control case that the current code already gets right via \"LGTM\" \u2014 it guards against a rewrite that over-rotates on the failures and starts losing the numeric-zero-plus-LGTM shape."
  },
  {
    "id": "gen-26",
    "expected": "clean",
    "body": "Reviewed PR #63 at `0aa9e31` and left no comments.\n\nI weighed one issue around the cache key in `src/server/cache.ts` \u2014 it omits the locale, so two locales share an entry \u2014 but the affected surface is a static footer fragment, which puts it at severity 4, below the reporting bar. I have not opened it as a finding.\n\nEverything else in the diff is straightforward: a dependency bump and the lockfile it implies. Head CI is green.",
    "source": "generated",
    "why": "Nothing is outstanding \u2014 the bot explicitly declined to open the observation as a finding and posted no comment \u2014 but \"not opened it as a finding\" is a negation form the stripper does not model, so it reads as a reported finding and sends an agent hunting for a fix that was never requested."
  },
  {
    "id": "gen-27",
    "expected": "clean",
    "body": "Review complete \u2014 PR #66 at `5c72b04`.\n\nThe findings section of the dashboard is empty for this run, and that is accurate: I read all nine changed files and none of them cross the severity bar. The new `withRetry` helper is only used by the two idempotent handlers, which is the correct restriction.\n\n`git diff --check` passes and the branch is clean.",
    "source": "generated",
    "why": "\"The findings section is empty\" states the absence using the word itself with no \"no/zero/0\" prefix, which is precisely what the bare-mention fallback cannot survive."
  },
  {
    "id": "gen-28",
    "expected": "clean",
    "body": "**Review complete** \u2014 PR #72 at `abc1234`\n\n| Metric | Value |\n| --- | --- |\n| Files reviewed | 9 |\n| Findings (severity >= 7) | 0 |\n| Comments posted | 0 |\n\nThe worker now acknowledges the message after the write commits rather than before, which is what the previous round asked for. Nothing else in the diff needs attention.\n\n**[View on dashboard](https://blocks.team/beamtech/pr/72/runs/9402)**",
    "source": "generated",
    "why": "A markdown summary table puts the count after the label (\"Findings (severity >= 7) | 0\"), inverting the word order the negation regex requires, so a table that literally reports zero is read as findings."
  },
  {
    "id": "gen-29",
    "expected": "clean",
    "body": "Reviewed PR #29 at `b18e2f5`.\n\nNo findings, and no inline comments. The change is a documentation amendment plus one renamed constant, and every reference to the old name is updated.\n\nI could not run the Vitest command locally because `pnpm` is unavailable in this environment, so this is a static review; the head CI run is successful.",
    "source": "generated",
    "why": "Control case the current code handles: canonical \"No findings\" phrasing plus the tool-unavailable caveat that must never be mistaken for an unfinished review."
  },
  {
    "id": "gen-30",
    "expected": "clean",
    "body": "Done \u2014 I reviewed the pull request at `e4410cd`.\n\nNothing actionable. The schema migration is additive, the backfill is chunked with a resumable cursor, and the rollback path drops only the column it added. I checked that no query in `src/db/queries` selects `*` from the affected table.\n\n**[View on dashboard](https://blocks.team/beamtech/pr/75/runs/9503)**",
    "source": "generated",
    "why": "\"Nothing actionable\" differs from the hardcoded \"no actionable findings\" by two words, so the clean test's second clause fails even though the first matches \u2014 a finished review classed nonterminal on vocabulary alone."
  },
  {
    "id": "gen-31",
    "expected": "clean",
    "body": "Reviewed PR #48 at `3fa77c9`.\n\nI re-read the three findings from round two, but all of them are fixed at this head: the token is scrubbed from the log line, the timeout is passed through, and the retry no longer doubles the side effect. I have nothing new to raise and posted no comments on this round.\n\nCI is green for `3fa77c9`.",
    "source": "generated",
    "why": "The resolution is stated across a \"but\" and with \"all of them\" between \"are\" and \"fixed\", both of which defeat the stripping regex, leaving \"three findings\" to trigger a confident false 'findings' on a fully resolved head."
  },
  {
    "id": "gen-32",
    "expected": "clean",
    "body": "Reviewed PR #80 at `9d0c3ae` and approved it.\n\nThe extraction of `parseFrontmatter` into its own module is a pure move, the tests moved with it, and the public export list is unchanged. Looks good to me \u2014 nothing outstanding.\n\n**[View on dashboard](https://blocks.team/beamtech/pr/80/runs/9611)**",
    "source": "generated",
    "why": "Control case that passes today on \"looks good\" and an approval with no findings vocabulary at all \u2014 the shape a rewrite must keep classifying clean without leaning on the word \"findings\"."
  },
  {
    "id": "gen-33",
    "expected": "findings",
    "body": "Reviewed `9c41ea2` and the queue-drain changes.\n\nOne finding at severity 7. `apps/worker/src/drain.ts` acknowledges the SQS message before `flushBatch()` resolves, so if the process is killed between the ack and the flush the batch is dropped with no trace. Acking after the await, or routing a rejected flush to the dead-letter queue, would close it.\n\nI did not open an inline thread because the ack and the flush sit twelve lines apart and a comment would only anchor to one of them. `pnpm` is unavailable in this environment so the worker suite did not run; `git diff --check` passes.\n\n**[View on dashboard](https://blocks.team/pr/412/review/9c41ea2)**",
    "source": "generated",
    "why": "A finished single-finding verdict opened with a bare sha (\"Reviewed `9c41ea2`\") rather than \"Reviewed PR\", so it stresses the same isVerdict gap that hung 40 minutes on [C] \u2014 the current code calls it nonterminal 'requested'."
  },
  {
    "id": "gen-34",
    "expected": "findings",
    "body": "Reviewed PR #47 at `b0f7d19`.\n\nThe path-traversal finding from round one is resolved: `resolveUpload()` now rejects any candidate that escapes the uploads root. One finding is still outstanding. The same handler derives the rate-limit key from `req.headers['x-forwarded-for']`, which the client controls, so rotating that header bypasses the limiter completely; take the socket address behind the trusted proxy hop instead.\n\nI summarised rather than commenting inline, since the line already carries an open thread from round one. Current head CI is green and `git diff --check` passes.\n\n**[View on dashboard](https://blocks.team/pr/47/review/b0f7d19)**",
    "source": "generated",
    "why": "One fixed finding and one outstanding finding in the same verdict: the fixed clause must be discounted without discounting the live one, and the current code happens to get this right, so it is a regression anchor for any rewrite."
  },
  {
    "id": "gen-35",
    "expected": "findings",
    "body": "Reviewed PR #52 at `3ad9c11`.\n\nThe finding about the missing retry budget in the billing retry helper is still open and I would block the merge on it \u2014 the earlier concern about debug logging in that same file has been addressed. Without a budget a provider 500 retries forever at the queue's redelivery rate, and the customer's card is charged on every attempt that does succeed.\n\nNothing is posted inline this round: the hunk moved, and GitHub anchors the comment to the wrong line.\n\n**[View on dashboard](https://blocks.team/pr/52/review/3ad9c11)**",
    "source": "generated",
    "why": "The outstanding finding is stated first and a resolved concern trails it in the same sentence, so the fixed-stripping regex swallows the span from \"finding\" all the way to \"has been addressed\" and the live finding vanishes \u2014 the current code returns a false 'clean'."
  },
  {
    "id": "gen-36",
    "expected": "findings",
    "body": "Reviewed PR #61 at `de92f04`.\n\nTwo severity >=7 findings, both in `src/auth/token.ts`:\n\n1. `verifyToken()` compares the signature with `===`, which leaks timing information. Use `crypto.timingSafeEqual` over equal-length buffers.\n2. The refresh path accepts a token with no `exp` claim at all, because `undefined < now` is false and the expiry check passes silently.\n\nNeither is posted inline \u2014 they live in a block this diff rewrites wholesale, so the anchors kept landing on the deleted side. Focused tests were not runnable here (`pnpm` missing).\n\n**[View on dashboard](https://blocks.team/pr/61/review/de92f04)**",
    "source": "generated",
    "why": "The positive mirror of [D]'s severity phrasing \u2014 \"Two severity >=7 findings\" \u2014 which the count regex cannot parse (\">=7\" is not \\w+) and which only survives today via the bare-mention fallback a repair is likely to delete."
  },
  {
    "id": "gen-37",
    "expected": "findings",
    "body": "Review complete for `1e77b3a`.\n\nI found three problems worth fixing before this merges. The migration in `db/migrations/0042_add_org_id.sql` adds a NOT NULL column with no default to a table that already has rows, so it fails on any non-empty database. The backfill it pairs with is wired to run after that migration rather than before. And `OrgContext` reads `org_id` straight off the session without checking the session belongs to that org, so a stale cookie reads across tenants.\n\nNo inline comments \u2014 all three are cross-file and read better together. `git diff --check` passes.\n\n**[View on dashboard](https://blocks.team/pr/58/review/1e77b3a)**",
    "source": "generated",
    "why": "A clearly finished verdict reporting three defects without ever using the word \"finding\", so reportsFindings sees nothing and the current code emits a false 'clean' that would send an agent to merge unreviewed problems."
  },
  {
    "id": "gen-38",
    "expected": "findings",
    "body": "Reviewed `8bd4a19` and the round-four diff.\n\nNo new severity >=7 findings in the migration itself, but the finding I raised on the backfill script in round two is still present: it pages with `LIMIT 1000` and no stable cursor, so rows shift underneath it and roughly a third of the table is skipped on a live database. That remains the blocking item on this PR.\n\nNothing new posted inline. CI on this head is green and `git diff --check` passes.\n\n**[View on dashboard](https://blocks.team/pr/33/review/8bd4a19)**",
    "source": "generated",
    "why": "Deliberate near-twin of the genuinely clean [C]/[D] wording \u2014 same \"No new severity >=7 findings\" opener \u2014 but a \"but\" clause carries an unresolved carry-over, so a fix that simply learns to treat that phrase as clean will invert this one."
  },
  {
    "id": "gen-39",
    "expected": "findings",
    "body": "Reviewed PR #33 at `05e6ca7`.\n\nZero new findings this round \u2014 the diff is limited to `README.md` and the release workflow. The item from round one is untouched, though: `web/app/(dashboard)/layout.tsx` still calls `cookies()` above the `Suspense` boundary, which opts the entire dashboard out of static rendering. Until that is fixed or explicitly waived I cannot call this branch clean.\n\nI have not re-posted the comment; the round-one thread is still open and unresolved.\n\n**[View on dashboard](https://blocks.team/pr/33/review/05e6ca7)**",
    "source": "generated",
    "why": "Zero new findings but an unresolved carry-over means issues are still outstanding, and the negation stripper removes the only \"findings\" token, so the current code returns a false 'clean' on a PR that is not clean."
  },
  {
    "id": "gen-40",
    "expected": "findings",
    "body": "Reviewed `7ac0f5b`.\n\nSeveral findings on the CSV export path, all under `apps/reports/src/export`:\n\n- The row stream is piped to the response with no back-pressure handling, so a 200k-row export buffers the whole file in memory.\n- `escapeCell()` does not prefix cells beginning with `=`, `+`, `-` or `@`, so a crafted value executes as a formula when the export is opened in Excel.\n- The temp file written under `/tmp` is never unlinked when the request aborts.\n\nI described them here instead of inline because they are three symptoms of one helper written against a small fixture. Tests were not runnable in this environment.\n\n**[View on dashboard](https://blocks.team/pr/71/review/7ac0f5b)**",
    "source": "generated",
    "why": "An unmistakable multi-finding verdict \u2014 \"Several findings\" plus a bulleted list \u2014 that the current code still calls nonterminal purely because the opener is a bare sha, proving the verdict test, not the findings test, is the hang risk here."
  },
  {
    "id": "gen-41",
    "expected": "findings",
    "body": "Reviewed this pull request end to end at `c72d4a0`.\n\nI left no inline comments, because both problems are structural rather than line-level. First, `useSyncQueue` subscribes in an effect but unsubscribes in a cleanup that closes over the first `queueId`, so every queue switch leaks a listener. Second, the retry wrapper swallows `AbortError`, which makes a cancelled navigation look like a completed sync and clears the dirty flag.\n\nBoth should be fixed before merge. `git diff --check` passes and the head CI run is green.\n\n**[View on dashboard](https://blocks.team/pr/29/review/c72d4a0)**",
    "source": "generated",
    "why": "Hits both halves of isClean at once (\"Reviewed this pull request end to end\" and \"I left no inline comments\") while reporting two blocking defects in prose, the worst false 'clean' in the set because absence of inline comments is being used as evidence of cleanliness."
  },
  {
    "id": "gen-42",
    "expected": "findings",
    "body": "Re-review complete for `f3c81b2`.\n\nThe tracing guard I marked as cleared last round is back in its original shape: `isRouteEntry()` matches on the directory name alone again, so a shared helper under `app/` is approved even when a dynamic route imports it. Same defect as round two, reintroduced by the merge from `main` at `2b98c40`.\n\nNothing posted inline \u2014 the round-two thread is where that discussion already lives, and I would rather not fork it.\n\n**[View on dashboard](https://blocks.team/pr/29/review/f3c81b2)**",
    "source": "generated",
    "why": "A recognised verdict reporting a regression using \"cleared\" language and no plural \"findings\" anywhere, so the current code reads the reappeared defect as resolved and returns 'clean'."
  },
  {
    "id": "gen-43",
    "expected": "findings",
    "body": "Reviewed `bb417c9` and the two commits sitting on top of the round-three head.\n\n### Findings (2)\n\n**`packages/cli/src/config.ts`** \u2014 `loadConfig()` merges the user file over the defaults with `Object.assign`, so a nested key that exists in the defaults but not in the user file is dropped rather than inherited. Every project that sets `build.target` loses `build.sourcemap`.\n\n**`packages/cli/src/run.ts`** \u2014 the `--dry-run` flag is read after the first write to disk, so a dry run still truncates the lockfile.\n\nNeither is posted as an inline comment; the dashboard link carries the same detail.\n\n**[View on dashboard](https://blocks.team/pr/64/review/bb417c9)**",
    "source": "generated",
    "why": "Structured markdown with a \"### Findings (2)\" heading and a parenthesised count that no count regex reaches, on a bare-sha opener \u2014 it tests whether headings and formatting are read at all, since the current code stops at nonterminal."
  },
  {
    "id": "gen-44",
    "expected": "findings",
    "body": "Reviewed PR #58 at `aa10de3`.\n\nOne blocking finding and two nits. The blocking one: `withTenant()` memoises the resolved tenant in a module-level cache keyed by slug and never invalidates on rename, so after a tenant is renamed requests keep resolving to the stale row until the process restarts.\n\nThe nits, which I would not hold the PR for: the new test file re-declares the fixture that already exists in `test/fixtures`, and the README still documents the old flag name.\n\nNothing inline this round.\n\n**[View on dashboard](https://blocks.team/pr/58/review/aa10de3)**",
    "source": "generated",
    "why": "Mixes one blocking finding with two explicitly non-blocking nits, so a repair that starts discounting hedged or low-severity language must still keep the blocking one; the current code is right here and should stay right."
  },
  {
    "id": "gen-45",
    "expected": "findings",
    "body": "Reviewed PR #29 at `5e0b8d1`.\n\nThe filename guard now covers the shared-helper case from the previous round, and I left no additional PR comments. I did hit one new severity 8 issue while re-reading it: `normalisePath()` lowercases before comparing, so on a case-sensitive checkout `App/Page.tsx` and `app/page.tsx` collapse to the same entry and the second one is silently skipped by the guard.\n\nFocused local test execution was not possible; this checkout has neither `pnpm` nor dependencies installed. `git diff --check` passes.\n\n**[View on dashboard](https://blocks.team/pr/29/review/5e0b8d1)**",
    "source": "generated",
    "why": "Deliberately shaped like the genuinely clean [B] \u2014 same opener, same \"left no additional PR comments\" and same environment caveat \u2014 but with one severity 8 issue reported in prose, so it separates the wording that made [B] clean from the substance that makes this one findings."
  },
  {
    "id": "gen-46",
    "expected": "clean",
    "body": "**Review complete** for `9f14ac2`.\n\nI found no new severity >=7 correctness, security, or data-integrity findings in the four files this branch touches. The `retry-budget` helper is only reachable from the worker entrypoint, and the added test covers the exhausted-budget path. I left no inline comments.\n\nLocal verification was limited: `pnpm` is unavailable in this environment, so I read `vitest.config.ts` rather than running the suite. `git diff --check` passes.\n\n**[View on dashboard](https://blocks.team/pr/41/review/9f14ac2)**",
    "source": "generated",
    "why": "A finished review that posted nothing, but the negation is separated from \"findings\" by a five-token noun phrase containing `>=7`, so the two-word negation stripper misses it and the bare-mention fallback reports findings (current output: findings)."
  },
  {
    "id": "gen-47",
    "expected": "clean",
    "body": "Reviewed PR #41 at `c0ffee1`.\n\nNone of the changed files produced findings above the severity >=7 bar. The migration is additive, the backfill script is idempotent, and the rollback note in `docs/ops/migrations.md` matches what the migration actually does.\n\nI did not post inline comments on this round. CI is green for this exact head SHA.\n\n**[View on dashboard](https://blocks.team/pr/41/review/c0ffee1)**",
    "source": "generated",
    "why": "Nothing is outstanding, but both negations are lexical variants the stripper does not know \u2014 \"None of\" is not `\\bno\\b` and \"did not post inline comments\" is not \"no inline comments\" \u2014 so two clean statements each survive as evidence of findings (current output: findings)."
  },
  {
    "id": "gen-48",
    "expected": "clean",
    "body": "Re-review complete for `3ab99f0`.\n\nThe diff is free of blocking findings. The `session` cookie is now set with `sameSite: 'lax'` on both branches of the auth callback, which is what round two asked for, and no other call site sets it.\n\nI posted no inline comments this round. `git diff --check` passes and the head CI run is successful.\n\n**[View on dashboard](https://blocks.team/pr/41/review/3ab99f0)**",
    "source": "generated",
    "why": "\"Free of\" expresses the negation with no negative token at all, so the phrase survives stripping and trips the bare mention even though the adjacent sentence explicitly says no inline comments were posted (current output: findings)."
  },
  {
    "id": "gen-49",
    "expected": "clean",
    "body": "Reviewed PR #41 at `77c1d2e`.\n\nFor context, my round-two comment read:\n\n> Two findings at severity >=7, both in the dynamic-route guard.\n\nNeither survives at this head. The guard resolves the route entry before it approves a shared helper, and the second case is unreachable now that the helper is no longer exported. I have nothing to raise this round and left no inline comments.\n\n**[View on dashboard](https://blocks.team/pr/41/review/77c1d2e)**",
    "source": "generated",
    "why": "The only counted findings live inside a markdown blockquote of a previous round that the following sentence retracts, so a classifier that does not scope quotations reads \"Two findings\" as this round's verdict (current output: findings)."
  },
  {
    "id": "gen-50",
    "expected": "clean",
    "body": "Reviewed PR #41 at `5d2e88a`.\n\nThe one finding I raised in round three - the unguarded `JSON.parse` in `app/api/webhook/route.ts` - has been resolved by the new schema check, and the failure path now returns a 400 instead of throwing into the route handler.\n\nNothing else in this diff clears the severity >=7 bar, so I left no inline comments.\n\n**[View on dashboard](https://blocks.team/pr/41/review/5d2e88a)**",
    "source": "generated",
    "why": "The finding is explicitly resolved, but the backticked path between subject and verb contains dots (`JSON.parse`, `route.ts`) that break the resolution stripper's no-sentence-punctuation gap, leaving \"one finding\" to be counted (current output: findings)."
  },
  {
    "id": "gen-51",
    "expected": "clean",
    "body": "Review complete for `b41f0aa`.\n\nZero P1 or P2 findings. The two nits I mentioned in round one were style-only and I withdrew them, so nothing is outstanding from my side.\n\n`git diff --check` passes. I could not run the focused suite because dependencies are not installed in this checkout.\n\n**[View on dashboard](https://blocks.team/pr/41/review/b41f0aa)**",
    "source": "generated",
    "why": "\"Zero\" is a recognised negation but three tokens (\"P1 or P2\") sit before \"findings\", one past the stripper's limit, so an explicit zero-count reads as a positive count (current output: findings)."
  },
  {
    "id": "gen-52",
    "expected": "clean",
    "body": "Re-review complete for `e7c3d10`.\n\nThe rebase pulled in three upstream commits and the branch still passes without new findings at severity >=7. I checked the moved `lib/flags/` helpers against every new import site and the resolution order is unchanged.\n\nNo inline comments from me on this round.\n\n**[View on dashboard](https://blocks.team/pr/41/review/e7c3d10)**",
    "source": "generated",
    "why": "\"Without\" is an unrecognised negation sitting directly in front of the quantifier list's own word \"new\", so the counting regex matches \"new findings\" as an assertion rather than a denial (current output: findings)."
  },
  {
    "id": "gen-53",
    "expected": "clean",
    "body": "Reviewed PR #41 at `1c9b74f`.\n\nNothing in this round meets the severity >=7 bar. The findings list for the run is empty, and the only change since `4248476` is the changelog entry, which matches the merged commits one for one.\n\n`git diff --check` passes.\n\n**[View on dashboard](https://blocks.team/pr/41/review/1c9b74f)**",
    "source": "generated",
    "why": "The negation is carried by \"Nothing\" in one sentence and by \"is empty\" trailing the noun in the next, so \"findings\" appears as a bare container noun with its emptiness stated after it (current output: findings)."
  },
  {
    "id": "gen-54",
    "expected": "clean",
    "body": "Reviewed PR #41 at `0ab77c9`.\n\nQuoting my earlier verdict so the history sits in one place:\n\n> Three findings at severity >=7, all in `lib/queue/consumer.ts`.\n\nAll three are addressed at this head. The consumer acks after the handler resolves, the dead-letter path has a test, and the retry ceiling is read from config rather than hard-coded. I have no further comments.\n\n**[View on dashboard](https://blocks.team/pr/41/review/0ab77c9)**",
    "source": "generated",
    "why": "Combines the two hardest clean shapes \u2014 a quoted prior-round count and a resolution sentence \u2014 and the resolution stripper cannot bridge them because a dotted filename and a blockquote newline sit in the gap (current output: findings)."
  },
  {
    "id": "gen-55",
    "expected": "clean",
    "body": "Reviewed PR #41 at `44b1e02`.\n\n| Check | Result |\n| --- | --- |\n| Severity >=7 findings | 0 |\n| Inline comments posted | 0 |\n| `git diff --check` | pass |\n| Head CI | success |\n\nThe diff is a version bump plus the regenerated lockfile, and the two are consistent with each other.\n\n**[View on dashboard](https://blocks.team/pr/41/review/44b1e02)**",
    "source": "generated",
    "why": "A markdown summary table states both counts as zero, but the zero follows its noun instead of preceding it, so every negation pattern that assumes \"no/zero/0 <noun>\" word order fails and two explicit zeroes read as findings (current output: findings)."
  },
  {
    "id": "gen-56",
    "expected": "clean",
    "body": "Review complete for `2fd9c31`.\n\nOther than the documentation nit I withdrew last round, there are no severity >=7 findings on this branch. The new `useDeferredValue` call sits inside a client component and the server component above it is untouched.\n\nI left no inline comments. CI is green at this head.\n\n**[View on dashboard](https://blocks.team/pr/41/review/2fd9c31)**",
    "source": "generated",
    "why": "The canonical `>=7` seam plus a leading \"Other than ...\" concession clause that a naive fix might read as an exception carving a finding out of the negation, when it is withdrawing one (current output: findings)."
  },
  {
    "id": "gen-57",
    "expected": "clean",
    "body": "Reviewed PR #41 end to end at `9c30ff4`.\n\nStatic analysis surfaced no severity-7-or-above findings, and the dependency diff is empty apart from the pinned patch bump.\n\nI left no inline comments; `git diff --check` passes.\n\n**[View on dashboard](https://blocks.team/pr/41/review/9c30ff4)**",
    "source": "generated",
    "why": "A hyphenated threshold (\"severity-7-or-above\") is a single token to a reader but breaks `\\w+\\s+` word counting, so the negation is lost and the verdict path overrides the otherwise-correct clean signals in the same body (current output: findings)."
  },
  {
    "id": "gen-58",
    "expected": "clean",
    "body": "Re-reviewed `8e1c0b4` and its test-only diff.\n\nNo new severity >=7 issues, so I left no PR comments. The added cases cover both the timeout and the abort path, and they fail against the pre-fix helper, which is what I wanted to see.\n\n**[View on dashboard](https://blocks.team/pr/41/findings/8e1c0b4)**",
    "source": "generated",
    "why": "The [C]-style hang \u2014 \"Re-reviewed `sha`\" matches no verdict pattern \u2014 booby-trapped so the obvious fix does not simply flip it to findings: the only occurrence of the word \"findings\" is inside the dashboard URL (current output: nonterminal requested)."
  },
  {
    "id": "gen-59",
    "expected": "clean",
    "body": "Review complete for `af0921c`.\n\nI re-ran the reasoning on the lock ordering and I am 8/10 confident the deadlock I described in round two cannot occur at this head: both call sites take `accounts` before `ledger`. That leaves no 8/10-or-higher findings on this branch.\n\nNo inline comments posted.\n\n**[View on dashboard](https://blocks.team/pr/41/review/af0921c)**",
    "source": "generated",
    "why": "A slashed confidence threshold in the negation gap (\"no 8/10-or-higher findings\") defeats word-based counting, and the stray digits invite a count-matcher to read \"8\" or \"10\" as a number of findings (current output: findings)."
  },
  {
    "id": "gen-60",
    "expected": "findings",
    "body": "Reviewed PR #41 at `6b0a91d`.\n\nThe two findings from round two are fixed. One new severity >=8 finding remains, and I could not attach it inline because the affected line sits outside the diff hunk: `packages/api/src/limiter.ts` increments the quota counter before the transaction commits, so a rollback leaves the quota consumed. Under concurrent retries this over-counts and locks the tenant out for the rest of the window.\n\nPlease treat that as outstanding; everything else in the diff is fine.\n\n**[View on dashboard](https://blocks.team/pr/41/review/6b0a91d)**",
    "source": "generated",
    "why": "The mirror image of the clean set \u2014 resolved prior findings plus one genuinely outstanding singular finding reported in prose because it could not be posted inline \u2014 and it is correct today only by accident (the \"8\" in \">=8\" is read as a count), so any fix that strips severity thresholds or trusts the \"are fixed\" clause turns it into a false clean."
  },
  {
    "id": "gen-61",
    "expected": "nonterminal",
    "body": "I have reviewed the documentation half of `d81f0c3` so far; the code half is still in progress.\n\nThe three findings from the previous round look addressed in the prose, but I have not re-run the route-guard analysis over `app/` yet. I will edit this comment once the full pass is done - treat it as a progress note rather than a verdict.",
    "source": "generated",
    "why": "Guards the repair for [C]: the body opens with \"I have reviewed ... `sha`\" and names three findings, so loosening the verdict test to any \"reviewed\" would terminate an explicitly unfinished pass and report findings that the author says only look addressed."
  },
  {
    "id": "gen-62",
    "expected": "findings",
    "body": "Reviewed PR #31 at `a4f0912`.\n\nI posted no inline comments on this round because both affected lines sit inside a generated block and the annotations API rejects them there. The two problems are written up here instead: `apps/api/src/list` reads the pagination cursor before it validates it, and the retry wrapper in `packages/queue/src/retry` swallows `AbortError`, so a cancelled request retries forever.\n\nI cannot submit a formal review from this environment, so treat this comment as the verdict for `a4f0912`.\n\n**[View on dashboard](https://blocks.team/pr/31/round-4)**",
    "source": "generated",
    "why": "Two severity-7-plus defects are named as blocking with no inline comments attached, so the only evidence is prose; \"no inline comments\" is stripped as a negation and the outstanding pair is described with nouns the finding-detector never looks for, yielding a false clean."
  },
  {
    "id": "gen-63",
    "expected": "findings",
    "body": "Reviewed PR #31 at `c72b4ea`.\n\nTwo findings remain open on the queue worker, and the shared-helper bypass from round two was fixed. The pair still standing: the dispatcher acknowledges the message before the handler settles, and the backoff ceiling is read from an env var that is unset in preview environments, so it falls back to 0ms.\n\nFocused tests could not run here because `pnpm` is unavailable in this environment.\n\n**[View on dashboard](https://blocks.team/pr/31/round-5)**",
    "source": "generated",
    "why": "It says outright that two findings remain open, but the resolution clause later in the same sentence lets the \"findings ... was fixed\" strip span swallow the open count along with it, so nothing survives to be counted."
  },
  {
    "id": "gen-64",
    "expected": "findings",
    "body": "LGTM apart from one blocker in `packages/queue/src/retry`: the wrapper retries on every rejection, 4xx responses from the upstream API included, so a permanently failing request burns the entire budget and then reports success.\n\nEverything else on `1f7c803` reads fine to me and `git diff --check` passes. Happy to look again once that branch is narrowed.\n\n**[View on dashboard](https://blocks.team/pr/44/round-2)**",
    "source": "generated",
    "why": "A named blocker is outstanding, but the bare token LGTM satisfies both halves of the clean test before any outstanding-work check runs, so an approving idiom overrides the exception that follows it."
  },
  {
    "id": "gen-65",
    "expected": "findings",
    "body": "Overall this looks good: the documentation half matches the recorded discussion and CI is green for `8b1de07`.\n\nThe retry path is the exception. `withRetry` treats a thrown `AbortError` as retryable, which turns a cancelled upload into an unbounded loop against the storage API. That is a correctness finding at severity 8 and I would not merge over it.\n\n**[View on dashboard](https://blocks.team/pr/44/round-3)**",
    "source": "generated",
    "why": "The verdict explicitly refuses merge over a severity 8 finding, yet the reassuring opener \"looks good\" wins the clean test outright \u2014 the phrase \"a correctness finding\" is never even consulted because clean is decided before findings are."
  },
  {
    "id": "gen-66",
    "expected": "findings",
    "body": "Reviewed PR #58 at `4f0aa11`.\n\nZero of these findings have been addressed; the new commit only reformats the files they sit in. The unbounded retry loop, the missing tenant check in `apps/api/src/admin`, and the swallowed `AbortError` all reproduce exactly as they did on round two.\n\n**[View on dashboard](https://blocks.team/pr/58/round-3)**",
    "source": "generated",
    "why": "Three findings reproduce unchanged, but \"findings have been addressed\" matches the resolution-stripping pattern verbatim while the leading \"Zero of these\" that inverts it is discarded, turning the strongest possible findings statement into a clean one."
  },
  {
    "id": "gen-67",
    "expected": "findings",
    "body": "Reviewed PR #58 at `d2c11ab`.\n\nNothing is fixed yet: one finding, the unbounded retry loop, is still open, while the round-two logging note was cleared earlier today. This commit touches only the README and the changeset file, so the code path I flagged is byte-identical to the head I reviewed on round two.\n\n**[View on dashboard](https://blocks.team/pr/58/round-4)**",
    "source": "generated",
    "why": "One finding is stated as still open and nothing is fixed, but the appositive keeps the sentence running to a later \"was cleared\", so the strip span eats \"finding, ... is still open, while ...\" and leaves a dangling \"one\"."
  },
  {
    "id": "gen-68",
    "expected": "findings",
    "body": "Reviewed PR #62 at `61c0d2b`.\n\nNo actionable findings in the documentation commit. The code commit is a different matter: `src/worker/dispatch` acknowledges the message before the handler resolves, so any crash inside the handler drops the job silently and the queue never retries it. I have written it up here rather than inline because the line sits in a vendored file.\n\n**[View on dashboard](https://blocks.team/pr/62/round-2)**",
    "source": "generated",
    "why": "The clean phrase is scoped to one half of the diff while a job-loss defect stands in the other, and a scope-blind negation strip plus a vocabulary gap (\"drops the job\", not \"finding\") reads the whole comment as clean."
  },
  {
    "id": "gen-69",
    "expected": "findings",
    "body": "Reviewed PR #62 at `77aa41e`.\n\nThe three findings from round two are now resolved and the new ones are listed under **Open items** below.\n\n**Open items**\n1. `apps/web/src/session` still writes the refresh token to `localStorage`, which the threat model for this PR explicitly rules out.\n2. The migration in `db/0142_add_tenant` drops the old column in the same transaction that backfills it, so a failure mid-backfill loses the data.\n\n**[View on dashboard](https://blocks.team/pr/62/round-3)**",
    "source": "generated",
    "why": "Two new defects are enumerated in a markdown list, but the sentence announcing them is welded to \"findings ... are now resolved\", so the strip removes the pointer to the list and the list itself uses no keyword the detector recognises."
  },
  {
    "id": "gen-70",
    "expected": "findings",
    "body": "Reviewed the diff end to end at `b0d4417` and left no inline comments \u2014 the annotations API rejected both because the lines are inside a generated file.\n\nThe two problems: `packages/tokens/src/build` writes outside its output directory when `--out` is relative, and the CLI exits 0 on a partial write, so CI goes green on a truncated bundle. Both are severity 7 or above by my read.\n\n**[View on dashboard](https://blocks.team/pr/70/round-1)**",
    "source": "generated",
    "why": "Absence of inline comments here is a tooling failure, not an absence of findings, yet \"reviewed ... end to end\" plus \"left no inline comments\" satisfies both halves of the clean test before the two severity-7 problems are read."
  },
  {
    "id": "gen-71",
    "expected": "findings",
    "body": "Reviewed this pull request at `3ce9d55`. No actionable findings in the test-only files.\n\nThe production change is where I would push back: `apps/api/src/webhook` verifies the signature after it has already parsed and dispatched the payload, so a forged body reaches the handler regardless of the check. That needs a fix before this merges.\n\n**[View on dashboard](https://blocks.team/pr/70/round-2)**",
    "source": "generated",
    "why": "A signature-bypass defect is called out as merge-blocking, but \"reviewed this pull request\" and the file-scoped \"no actionable findings\" together trip the clean test, which short-circuits before the outstanding item is considered."
  },
  {
    "id": "gen-72",
    "expected": "findings",
    "body": "Re-review complete for `9ad3e2c`: two findings stand, both in `apps/worker/src/dispatch`.\n\nSeparately I am re-running the dependency install so I can execute the focused tests as well; that may take a few minutes and I will edit this comment with the outcome. The verdict above does not depend on it.\n\n**[View on dashboard](https://blocks.team/pr/75/round-2)**",
    "source": "generated",
    "why": "A completed re-review naming two standing findings is demoted to nonterminal because an unrelated background note contains a courtesy phrase, and the courtesy test is allowed to veto an explicit completion marker."
  },
  {
    "id": "gen-73",
    "expected": "findings",
    "body": "Review complete for `9ad3e2c`.\n\nOne severity 8 issue is outstanding: `packages/auth/src/verify` compares the signature with `===` on a string, so it is timing-attackable, and the constant-time helper is already imported two lines above. Everything else on this head is fine by me and `git diff --check` passes.\n\n**[View on dashboard](https://blocks.team/pr/75/round-3)**",
    "source": "generated",
    "why": "This is a finished review with one outstanding severity 8 item, but it never uses the words \"finding\" or \"inline comment\", so a detector keyed to those two nouns finds nothing outstanding and reports clean."
  },
  {
    "id": "gen-74",
    "expected": "clean",
    "body": "Reviewed PR #58 at `9c1e0aa`.\n\nI found nothing at severity >=7 on this head. The three findings I raised on round two are gone: the retry loop is bounded, the tenant check is in place, and `AbortError` is rethrown. I left no PR comments.\n\n`git diff --check` passes and the CI run for this exact head SHA is green.\n\n**[View on dashboard](https://blocks.team/pr/58/round-5)**",
    "source": "generated",
    "why": "Nothing is outstanding \u2014 the historical findings are enumerated only to confirm they are gone \u2014 so it guards against overcorrecting: a repair that treats any surviving mention of \"findings\" as outstanding sends an agent hunting for a finding that no longer exists."
  },
  {
    "id": "gen-75",
    "expected": "clean",
    "body": "Reviewed `2f5aa30` and the follow-up commit on this branch.\n\nEvery inline comment I left earlier is resolved, and I found nothing new at severity >=7, so I left no comments this round. Focused tests still could not run here \u2014 this checkout has no dependencies installed \u2014 but `git diff --check` passes and CI is green for this exact head SHA.\n\n**[View on dashboard](https://blocks.team/pr/58/round-6)**",
    "source": "generated",
    "why": "A finished review with nothing outstanding that names a SHA instead of \"PR\" and reports cleanliness as \"nothing new at severity >=7\" \u2014 the exact shape that hung the loop for forty minutes, kept here so the repair is not narrowed to the literal phrases in the observed clean bodies."
  },
  {
    "id": "gen-76",
    "expected": "nonterminal",
    "body": "Picking this back up after the force-push. My previous comment \u2014 \"Reviewed PR #29 at `4248476`\" \u2014 describes a head that no longer exists, so please disregard it for this round.\n\nI am re-reading the diff against `5b91cc2` now and will edit this comment with the result.\n\n**[View on dashboard](https://blocks.team/pr/29/round-6)**",
    "source": "generated",
    "why": "The review of the current head has not started, and the only verdict phrase in the body is a quotation of a superseded comment \u2014 a verdict matcher that ignores quoting and grammatical subject accepts an unreviewed head as clean."
  },
  {
    "id": "gen-77",
    "expected": "nonterminal",
    "body": "Starting on `d0c7f31` now. No findings yet \u2014 so far I have only walked the migration files. I will post the full result in this comment once the pass finishes.\n\n**[View on dashboard](https://blocks.team/pr/81/round-1)**",
    "source": "generated",
    "why": "This is a progress note whose \"no findings yet\" is provisional, but the phrase satisfies both halves of the clean test with no completion marker present, so an in-flight review is accepted as a finished clean one."
  },
  {
    "id": "real-H",
    "expected": "clean",
    "body": "Reviewed PR #29 at `0eefe942`.\n\nThe documentation correction is accurate; upstream PR #15 is still open, and the remaining wording consistently treats its fix as unmerged. No new severity \u22657 issues found, so I left no additional PR comments. CI for this head is successful.\n\n**[View on dashboard](https://blocks.team/app/dd73b09d/sessions/8eb593e7)**",
    "source": "observed on cueplusplus/skills#29, round 6",
    "why": "A clean verdict that says a PULL REQUEST is 'still open'. An unscoped still-open test reads that as an outstanding finding; the standing-predicate check must stay bound to a findings noun."
  }
]
